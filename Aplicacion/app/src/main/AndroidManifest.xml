<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ivanovic.teamsclasification">

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Main"
            android:label="Fútbol Abierto"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

        <activity android:name=".ClasificacionEspania"
            android:label="Liga Santander"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

        <activity android:name=".ClasificacionInglaterra"
            android:label="Premiere League"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

        <activity android:name=".ClasificacionItalia"
            android:label="Serie A"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

        <activity android:name=".ClasificacionFrancia"
            android:label="Ligue 1"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

        <activity android:name=".ClasificacionAlemania"
            android:label="Bundesliga"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

        <activity android:name=".ClasificacionGeneral"
            android:label="Clasificaciones"
            android:theme="@android:style/Theme.DeviceDefault.Light"/>

    </application>

</manifest>